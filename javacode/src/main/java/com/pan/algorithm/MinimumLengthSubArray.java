package com.pan.algorithm;

// Given an array, find a subarray such the sum of the elements in subarray is greater than or equal to target
//value. Find the minimus length of such subarray.
public class MinimumLengthSubArray {

    public static void main(String... args) {

    }

    // return a class with startIndex and length
    // traverse the array, i , sum(from i to j), if sum >= given target, store the smallest value of the subArray, next i,
    // if array[i] < smallest value, skip
}
